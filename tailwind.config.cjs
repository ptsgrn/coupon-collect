const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			fontFamily: {
				sans: ['IBM Plex Sans Thai', 'sans-serif']
			}
		}
	},

	plugins: []
};

module.exports = config;
